# GeminiSaver — pinned dependencies
#
# Phase 1 (proxy + Gemini passthrough) installs everything up to httpx.
# The caching / routing / dashboard layers (Phases 2-4) add the rest.
# Versions are pinned to what the build was verified against.

# --- Proxy core (Phase 1) ---
fastapi==0.141.1
uvicorn[standard]==0.53.0
google-genai==2.23.0
pydantic==2.13.5
pydantic-settings==2.15.0
python-dotenv==1.2.3
httpx==0.28.1

# --- Semantic cache (Phase 2) ---
sentence-transformers==3.3.1
faiss-cpu==1.9.0          # numpy brute-force cosine fallback if this fails to install
numpy==2.1.3

# --- Persistence (Phase 4) ---
aiosqlite==0.20.0

# --- Dashboard (Phase 4) ---
streamlit==1.40.2

# --- Tests ---
pytest==9.1.1
pytest-asyncio==1.4.0
